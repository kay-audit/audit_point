"""Настройки домена ЦК Code Mining."""

from pydantic import BaseModel


class CkCodeMiningSettings(BaseModel):
    """Корневая модель настроек домена ЦК Code Mining.

    В текущей итерации домен не использует собственных таблиц/полей —
    регистрируется только HTML-роут с информационной заглушкой. Settings
    оставлен как placeholder, чтобы соответствовать архитектуре плагина
    (домен всегда имеет settings_class) и чтобы будущие настройки
    (например, внешние ссылки на инструменты) можно было добавлять сюда
    без перерегистрации дескриптора.
    """

    schema_name: str = ""
